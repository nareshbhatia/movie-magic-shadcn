import { Check, ChevronDown, Heart, Moon, SunMedium } from 'lucide-react';
import type { LucideIcon } from 'lucide-react';

export type Icon = LucideIcon;

export const Icons = {
  check: Check,
  chevronDown: ChevronDown,
  heart: Heart,
  moon: Moon,
  sun: SunMedium,
};
